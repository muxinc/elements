---
import type { MuxPlayerProps } from './types';
import { toNativeAttributes } from './utils';
type Props = MuxPlayerProps;
const { theme, ...props } = Astro.props as Props;
const Theme = theme?.component;
---

<mux-player {...toNativeAttributes(props)} theme={theme?.name}><slot /></mux-player>
{Theme && <Theme />}
<script>
  import '@mux/mux-player';
</script>
