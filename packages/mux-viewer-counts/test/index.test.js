import { fixture, assert, aTimeout } from '@open-wc/testing';
import MuxViewerCount from '../src/index.ts';

describe('<mux-viewer-count>', () => {
  it('needs real tests', async function () {
    assert.true(true);
  });
});
