/** @TODO clean up build so we can use defs from playback-core (currently will result in double-bundling package) (CJP) */
export const StreamTypes = {
  VOD: 'on-demand',
  LIVE: 'live',
  LL_LIVE: 'll-live',
};
