import { fixture, assert, aTimeout } from '@open-wc/testing';
import MuxActiveViewerCount from '../src/index.ts';

describe('<mux-active-viewer-count>', () => {
  it('needs real tests', async function () {
    assert.isTrue(true);
  });
});
